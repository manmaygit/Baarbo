import 'package:flutter/material.dart';

const kRedWeWant = Color(0xFFFA4248);
const Color kBackgroundBlack = Color(0xFF2C2C2C);
var kBookAppointmentDecoration = BoxDecoration(
  border: Border.all(color:kRedWeWant),
  borderRadius: BorderRadius.all(Radius.circular(32.0)),
);
